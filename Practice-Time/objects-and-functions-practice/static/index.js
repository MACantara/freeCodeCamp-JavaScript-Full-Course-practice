let person = {
    name: "Mik",
    age: 22,
    country: "Philippines"
}

function logData() {
    console.log(person.name + " is " + person.age + " years old and lives in the " + person.country + ".");
}

logData();